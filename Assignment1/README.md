# Assignment 1 (UNO domain model)
## Structure
- `domain/` – UNO domain model and tests.

## Getting started
```bash
npm install               # installs the domain workspace
npm test -w domain        # run domain tests
```
